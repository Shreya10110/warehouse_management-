import { ArrowRight, Boxes, LoaderCircle, LockKeyhole, Mail, PackageCheck, Sparkles, Truck } from 'lucide-react'
import { useState } from 'react'
import { Link, Navigate, useNavigate } from 'react-router-dom'
import { useAuth } from '../context/AuthContext.jsx'
import { homeForRole } from '../utils/roleRoutes.js'

export default function LoginPage() {
  const { user, login } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ email: '', password: '' }); const [error, setError] = useState(''); const [submitting, setSubmitting] = useState(false)
  if (user) return <Navigate to={homeForRole(user.role)} replace />
  async function handleSubmit(event) { event.preventDefault(); setError(''); setSubmitting(true); try { const authenticatedUser = await login(form); navigate(homeForRole(authenticatedUser.role), { replace: true }) } catch (requestError) { setError(requestError.message) } finally { setSubmitting(false) } }
  return <main className="relative grid min-h-screen place-items-center overflow-hidden bg-slate-950 px-5 py-10 text-white"><div className="absolute -left-24 top-8 h-96 w-96 rounded-full bg-blue-600/35 blur-3xl" /><div className="absolute -right-20 bottom-0 h-[28rem] w-[28rem] rounded-full bg-cyan-400/20 blur-3xl" /><div className="absolute inset-0 opacity-20" style={{ backgroundImage: 'linear-gradient(#ffffff12 1px, transparent 1px), linear-gradient(90deg, #ffffff12 1px, transparent 1px)', backgroundSize: '42px 42px' }} /><section className="relative w-full max-w-5xl"><header className="flex items-center justify-between"><div className="flex items-center gap-3 text-xl font-black"><span className="rounded-2xl bg-blue-600 p-3 shadow-lg shadow-blue-600/30"><Boxes /></span> Whitfield Fulfillment</div><p className="hidden text-sm font-semibold text-blue-200 sm:block">Warehouse operations, in motion.</p></header><div className="mt-10 grid gap-8 lg:grid-cols-[1.2fr_.8fr] lg:items-center"><article><p className="flex items-center gap-2 text-sm font-bold uppercase tracking-[.25em] text-cyan-300"><Sparkles className="h-4 w-4" /> Shift control centre</p><h1 className="mt-4 max-w-3xl text-5xl font-black leading-[.95] tracking-tight sm:text-7xl">One warehouse.<br /><span className="text-blue-400">One rhythm.</span><br />Every move matters.</h1><p className="mt-6 max-w-xl text-lg leading-relaxed text-slate-300">Welcome to Whitfield Fulfillment. Receive with confidence, fulfil faster, and keep every decision visible to your team.</p><div className="mt-8 grid max-w-xl grid-cols-3 gap-3"><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><Truck className="h-5 text-cyan-300" /><p className="mt-3 text-sm font-bold">Receive</p></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><Boxes className="h-5 text-blue-300" /><p className="mt-3 text-sm font-bold">Organise</p></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><PackageCheck className="h-5 text-emerald-300" /><p className="mt-3 text-sm font-bold">Deliver</p></div></div></article><form onSubmit={handleSubmit} className="rounded-3xl border border-white/15 bg-white p-7 text-slate-950 shadow-2xl shadow-black/30 sm:p-9"><p className="text-xs font-bold uppercase tracking-[.2em] text-blue-600">Welcome back</p><h2 className="mt-2 text-3xl font-black">Ready for a strong shift?</h2><p className="mt-2 text-slate-500">Sign in to open your role-based workspace.</p>{error && <div role="alert" className="mt-5 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<label className="mt-6 block text-sm font-semibold">Email address<div className="relative mt-2"><Mail className="absolute left-3 top-3 h-5 w-5 text-slate-400" /><input type="email" autoComplete="email" required value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} className="w-full rounded-xl border border-slate-300 py-3 pl-11 pr-3 outline-none focus:border-blue-600 focus:ring-2 focus:ring-blue-100" placeholder="you@whitfield.com" /></div></label><label className="mt-5 block text-sm font-semibold">Password<div className="relative mt-2"><LockKeyhole className="absolute left-3 top-3 h-5 w-5 text-slate-400" /><input type="password" autoComplete="current-password" required minLength="8" value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} className="w-full rounded-xl border border-slate-300 py-3 pl-11 pr-3 outline-none focus:border-blue-600 focus:ring-2 focus:ring-blue-100" placeholder="Enter your password" /></div></label><button disabled={submitting} className="mt-7 flex w-full items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 py-3.5 font-bold text-white hover:bg-blue-700 disabled:opacity-60">{submitting && <LoaderCircle className="h-5 w-5 animate-spin" />}{submitting ? 'Opening your workspace...' : 'Enter my workspace'}{!submitting && <ArrowRight className="h-5 w-5" />}</button><div className="mt-6 border-t border-slate-200 pt-5 text-center text-sm text-slate-500">New manager or employee? <Link to="/signup" className="font-bold text-blue-600">Request access</Link></div></form></div></section></main>
}
