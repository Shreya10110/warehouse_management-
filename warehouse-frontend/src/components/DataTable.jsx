import { Search } from 'lucide-react'
import { useMemo, useState } from 'react'

export default function DataTable({ columns, rows = [], empty = 'No records found', actions }) {
  const [query, setQuery] = useState('')
  const filtered = useMemo(() => rows.filter((row) => JSON.stringify(row).toLowerCase().includes(query.toLowerCase())), [rows, query])
  return <div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-200 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="relative max-w-sm flex-1"><Search className="absolute left-3 top-2.5 h-4 w-4 text-slate-400" /><input aria-label="Search table" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search records…" className="w-full rounded-lg border border-slate-300 py-2 pl-9 pr-3 text-sm outline-none focus:border-blue-600 focus:ring-2 focus:ring-blue-100" /></div><p className="text-xs font-semibold text-slate-500">{filtered.length} records</p></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{columns.map((column, index) => <th key={`${column.key}-${index}`} className="px-5 py-3 font-semibold">{column.label}</th>)}{actions && <th className="px-5 py-3">Actions</th>}</tr></thead><tbody className="divide-y divide-slate-100">{filtered.map((row, rowIndex) => <tr key={row.id ?? rowIndex} className="hover:bg-slate-50/70">{columns.map((column, columnIndex) => <td key={`${column.key}-${columnIndex}`} className="px-5 py-4 text-slate-700">{column.render ? column.render(row[column.key], row) : (row[column.key] ?? '—')}</td>)}{actions && <td className="px-5 py-4">{actions(row)}</td>}</tr>)}{filtered.length === 0 && <tr><td colSpan={columns.length + (actions ? 1 : 0)} className="px-5 py-14 text-center text-slate-500">{empty}</td></tr>}</tbody></table></div></div>
}
